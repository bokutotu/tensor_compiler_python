## Codex利用時のルール

- Pythonの実行は **コードベースの動作確認** に限る。リポジトリ外の調査や補助計算には、Codexに備わっている他のツールを優先して使うこと。
- 変更を行ったら必ず `uv run basedpyright` を実行し、型エラーがないことを確認する。
- テストは `uv run pytest` で実行するのが標準。
- 質問、実装の依頼の際、どんな些細なことでも、疑問点や不明点があれば必ず質問すること。推測で動作させてはいけない。
- 知識が不足する場合は、Web検索を行うこと。
